stages:
- build
      
builder:
    stage: build
    image: debian:12
    script:
        - echo 'deb http://deb.debian.org/debian/ stable main contrib non-free' >> /etc/apt/sources.list
        - apt-get update && apt-get upgrade && apt-get install -y nodejs npm git curl wget wine mono-complete dpkg rpm zip unzip
        - npm install .
        - npm run make
        - npm run make-win
        - npm run make-darwin
    artifacts:
        paths:
            - ./out/make/*